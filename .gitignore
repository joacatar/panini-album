# —— Secretos (nunca commitear) ——
.env
.env.local
.env.*.local
frontend/.env
backend/.env

# —— Python ——
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/

# —— Node / frontend ——
node_modules/
frontend/node_modules/
dist/
frontend/dist/
*.tsbuildinfo

# —— IDE / editor ——
.idea/
.vscode/
*.swp
*.swo
.cursor/

# —— OS ——
.DS_Store
Thumbs.db

# —— Logs / temp ——
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# —— Certificados dev (vite basic-ssl) ——
*.pem

# —— Artefactos de seed/MCP (temporal; usar supabase/migrations + seed/*.sql) ——
supabase/seed/batches/

# —— Lockfile suelto en raíz (el real está en frontend/) ——
/package-lock.json
