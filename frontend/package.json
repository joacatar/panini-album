{
  "name": "panini-intercambios",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "node ../scripts/dev-auth-redirect.mjs & vite",
    "dev:host": "node ../scripts/dev-auth-redirect.mjs & vite --host",
    "dev:mobile": "node ../scripts/dev-auth-redirect.mjs & vite --host",
    "dev:https": "VITE_HTTPS=1 vite --host",
    "build": "vite build",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.47.0",
    "tesseract.js": "^6.0.1"
  },
  "devDependencies": {
    "@vitejs/plugin-basic-ssl": "^2.3.0",
    "vite": "^6.0.0"
  }
}
